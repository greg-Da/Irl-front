import { SafeAreaView, ScrollView, Text } from "react-native";

export default function Scanned(){

    return(
        <SafeAreaView>
            <ScrollView>
                <Text>Scanned</Text>
            </ScrollView>
        </SafeAreaView>
    )
}